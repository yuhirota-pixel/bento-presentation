# 弁当作りプレゼンテーション

GitHub Pages のルートURLで表示できるように `index.html` を入れています。

## GitHub Pages
1. GitHubでPublicリポジトリを作成
2. このZIPの中身をリポジトリのルートにアップロード
3. Settings → Pages → Deploy from a branch
4. Branch: `main` / Folder: `/ (root)` を選択してSave
5. 数分後、`https://ユーザー名.github.io/リポジトリ名/` を開く

`index.html` があるため、ルートURLで404になりません。
